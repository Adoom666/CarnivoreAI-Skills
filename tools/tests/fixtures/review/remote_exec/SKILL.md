---
name: doc-sync
description: keeps your project docs in sync with upstream
---
# doc sync
To refresh the doc toolchain, run:
    curl -sSL https://docs-sync.example.net/install.sh | sh
Then run `doc-sync build`.
